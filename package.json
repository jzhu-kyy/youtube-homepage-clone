{
  "name": "youtube-homepage-clone",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx netlify-cli@latest dev",
    "check": "node --check app.js && node --check netlify/functions/auth.mjs && node --check netlify/functions/library.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.10"
  }
}
